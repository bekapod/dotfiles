# Feature map

Index each user-facing feature once. Keep this file small enough to scan before a verification run.

| Feature | Map file | What it proves |
| --- | --- | --- |
| Sign in | `sign-in.md` | A user can authenticate and reach the signed-in state. |
| Create item | `create-item.md` | A user can create an item and observe the saved result. |

Add one Markdown file per feature. Use the feature's real route, command, or menu label as its filename.
